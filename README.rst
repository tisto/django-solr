Django Haystack Starter
=======================

Python Setup::

  $ virtualenv .env
  $ source .env/bin/activate
  $ pip install -r requirements.txt

Create Django App::

  $ django-admin startproject mysite
  $ (cd mysite && python manage migrate)

Start Django::

  $ (cd mysite && python manage runserver)
